# 项目设置说明

## 🚀 快速开始

由于Gradle Wrapper需要实际的JAR文件，请按以下步骤操作：

### 方法一：使用Android Studio（推荐）

1. **打开Android Studio**
2. **选择 "Open an existing Android Studio project"**
3. **选择 `ArithScreenLock` 文件夹**
4. **等待Android Studio自动下载和配置Gradle**
5. **点击 "Sync Project with Gradle Files"**

Android Studio会自动：
- 下载正确的Gradle版本
- 下载所有依赖
- 配置项目

### 方法二：手动下载Gradle Wrapper

如果你想使用命令行：

1. **下载Gradle Wrapper JAR**
   ```bash
   # 在项目根目录执行
   curl -L https://github.com/gradle/gradle/raw/v8.0.0/gradle/wrapper/gradle-wrapper.jar -o gradle/wrapper/gradle-wrapper.jar
   ```

2. **或者从现有Android项目复制**
   - 复制任何现有Android项目的 `gradle/wrapper/gradle-wrapper.jar`
   - 粘贴到本项目的 `gradle/wrapper/` 目录

3. **然后运行**
   ```bash
   ./gradlew build
   ```

## 📱 项目功能

✅ **已完成的功能：**
- 数学题解锁系统（20以内加减法）
- 控制台设置（密码：123456）
- 家长模式PIN码验证
- 自动锁屏服务
- 开机自启动
- 完整的Android项目结构

## 🔧 项目配置

- **Gradle版本**: 8.0
- **Android Gradle Plugin**: 8.0.2
- **Kotlin版本**: 1.8.20
- **目标SDK**: 33
- **最低SDK**: 24

## 📁 项目结构

```
ArithScreenLock/
├── app/
│   ├── src/main/java/com/example/arithscreenlock/
│   │   ├── MainActivity.kt           # 主界面
│   │   ├── LockScreenActivity.kt     # 锁屏界面
│   │   ├── SettingsActivity.kt       # 设置界面
│   │   ├── LockScreenService.kt      # 锁屏服务
│   │   ├── MathQuestion.kt           # 数学题生成
│   │   └── ...
│   └── src/main/res/                 # 资源文件
├── gradle/wrapper/                   # Gradle配置
└── build.gradle                      # 构建配置
```

## 🎯 使用说明

1. **启动应用** → 点击"启动锁屏服务"
2. **设置参数** → 点击"设置"，输入密码123456
3. **解锁屏幕** → 解答数学题或使用家长模式
4. **家长模式** → 右上角按钮，输入PIN码

## 🔐 PIN码规则示例

- 提示码 `1532` → 从第1位开始 → 答案 `1532`
- 提示码 `2351` → 从第2位开始 → 答案 `3512`
- 提示码 `3456` → 从第3位开始 → 答案 `5634`

现在项目已经完全配置好，可以在Android Studio中正常构建和运行！